services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: shopsphere_backend

    ports:
      - "5000:5000"

    env_file:
      - .env

    restart: unless-stopped